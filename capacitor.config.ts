import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seucontrole.financeiro',
  appName: 'Controle Financeiro',
  webDir: 'www'
};

export default config;
